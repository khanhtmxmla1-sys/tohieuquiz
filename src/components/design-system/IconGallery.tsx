import { ModuleIcon, MODULE_ICON_CATALOG, MODULE_ICON_SIZES } from '../common';
import type { ModuleIconName, ModuleIconSize } from '../common';
const good = 'Tiêu đề module, card chức năng, empty state, onboarding và giới thiệu tính năng.';
const bad = 'Sidebar, nút sửa/xóa/đóng, input, bảng dữ liệu, badge nhỏ và menu thao tác.';
export const IconGallery = () => <section aria-labelledby="module-icon-title" className="space-y-6"><div><h2 id="module-icon-title" className="text-2xl font-semibold text-slate-900">Icon module</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-600">Chín icon minh họa cấp cao của TôHiệuQuiz. Các icon thao tác nhỏ tiếp tục dùng Lucide.</p></div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{(Object.keys(MODULE_ICON_CATALOG) as ModuleIconName[]).map(name => { const item = MODULE_ICON_CATALOG[name]; return <article key={name} className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="border-b border-slate-100 p-5"><h3 className="font-semibold text-slate-900">{item.label}</h3><code className="mt-1 block text-xs text-slate-500">{name}</code></div><div className="grid grid-cols-2 gap-3 p-5">{(Object.keys(MODULE_ICON_SIZES) as ModuleIconSize[]).map(size => <div key={size} className="flex min-h-28 flex-col items-center justify-center rounded-xl bg-slate-50 p-3"><ModuleIcon name={name} size={size} /><span className="mt-2 text-xs font-medium text-slate-500">{size} · {MODULE_ICON_SIZES[size]}px</span></div>)}</div><div className="space-y-3 border-t border-slate-100 p-5 text-sm leading-6"><p><strong className="text-emerald-700">Nên dùng:</strong> {good}</p><p><strong className="text-rose-700">Không dùng:</strong> {bad}</p><pre className="overflow-x-auto rounded-lg bg-slate-950 px-3 py-2 text-xs text-slate-100"><code>{`<ModuleIcon name="${name}" size="md" />`}</code></pre></div></article>; })}</div></section>;
