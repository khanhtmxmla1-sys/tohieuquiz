# Production release record — main-13d9ac0

**Release date:** 2026-07-30  
**Source:** merge commit `13d9ac0` from PR #7  
**Scope:** modernization Tasks 1–37, excluding Task 38 production cleanup

## Release controls

- PR approval, required checks, security checks and `Release ready` all passed before merge.
- An encrypted D1 backup and private Time Travel bookmarks were captured outside Git before schema mutation.
- Frontend production deployment completed through Vercel.
- D1 migrations advanced to 53 entries, latest `0054_feature_rollout_control_plane.sql`, with no pending migration.
- Worker candidate `82889eeb…` was uploaded from `13d9ac0` with `AUTH_MIGRATION_MODE=compat`, preserving the production auth observation boundary.
- Rollback Worker `36ef84fc…` remains available in deployment history.

## D1 verification

Baseline business row counts were unchanged after migration:

| Dataset | Before | After |
|---|---:|---:|
| Teachers | 4 | 4 |
| Students | 3 | 3 |
| Results | 4 | 4 |

The final schema contained two feature flags, two rollout rules and the six canonical Gift Shop governance triggers. The canonical migration audit completed successfully against 88 tables.

Migration `0049` exposed a Wrangler migration-apply parsing limitation for valid trigger SQL. The release used D1 file ingest with the original SQL and registry insert in the same file, verified first on isolated remote D1 databases. The permanent, dry-run-first wrapper is documented in [`../d1-safe-migrations.md`](../d1-safe-migrations.md).

## Worker rollout evidence

1. Candidate at 0%: direct version-override health, CORS, hostile-origin, auth-policy, Operations and Passkey checks passed.
2. Canary at 10%: 60 monitoring rounds over 15 minutes, zero failures, p95 170 ms.
3. Routing sample: 9 candidate responses out of 100, zero errors and zero unknown responses.
4. Promotion to 100%: 20 monitoring rounds over five minutes, zero failures, p95 348 ms.
5. Public production browser smoke: 3/3 passed across canonical, apex and Parent Portal shells.

Authenticated role smoke was not run because the protected GitHub environment did not yet contain dedicated smoke-account secrets. Owner credentials were not substituted. This is recorded as an operational follow-up, not hidden as a successful check.

## Open boundaries

- Task 10 remains open. Production continues in `AUTH_MIGRATION_MODE=compat`; the 72-hour zero-legacy observation window began after the 100% Worker promotion at 2026-07-30 01:22:48 UTC (2026-07-30 08:22:48 UTC+7). Enforce mode and legacy-path deletion require their separate evidence windows.
- Task 38 cleanup was not performed. No test account, class or production artifact was deleted.
- Full bookmarks, database UUIDs, encrypted archives, passphrases, cookies and credentials are deliberately excluded from Git.
