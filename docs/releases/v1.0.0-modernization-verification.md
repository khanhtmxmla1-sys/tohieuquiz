# TôHiệuQuiz v1.0.0 modernization verification

## Release status

**State:** PREPARED — production cleanup and post-cleanup smoke are still pending.

This record closes the 38-task modernization plan only after every production operation below has evidence. It must not be changed to `RELEASED` before the cleanup, smoke and final verification succeed.

## Source and deployed baseline

- Release branch baseline: `origin/main@ed2503d12954dcfbecf7a03c416b9419eeea032d`.
- Production API Worker before Task 38 cleanup: `tohieuquiz-api` version `96705980-78e2-4b5b-89f2-883a989dfec7`, 100% traffic.
- Reviewed Worker rollback version: `2003f752-22fd-4503-a05f-6c377ebfc08a`.
- Vercel production source before Task 38: `ed2503d12954dcfbecf7a03c416b9419eeea032d`.
- Latest modernization D1 migration in source: `0054_feature_rollout_control_plane.sql`.
- Task 10 auth observation gates were closed early by owner risk override; the separate release records remain authoritative for that exception.

## Task 38 preparation evidence

- Cleanup script: `workers/scripts/cleanup-production-test-data.cjs`.
- Runbook: `docs/operations/production-test-data-cleanup.md`.
- TDD: initial failure because the script did not exist; final cleanup contract 4/4 passed.
- D1 safety regression: cleanup, backup, safe-migration, migration-layout and rollback suites passed 5 files/33 tests.
- ESLint and security scan passed.
- Production dry-run passed and preserved `smoke.student` plus `thienkhanh`.
- Dry-run artifact counts:
  - quizzes: 1;
  - assignments: 2;
  - results: 4;
  - certificate batches/certificates: 1/2;
  - live-exam sessions: 1;
  - result report/batches: 1/2;
  - notifications/parent notifications: 6/6.
- Planned private R2 deletion: two generated certificate PNG objects; the default certificate template/background is excluded.

## Required final evidence

The following fields remain pending until the operation is complete:

- [ ] Private pre-cleanup D1 Time Travel bookmark captured.
- [ ] Reviewed cleanup PR merged to `main`.
- [ ] Production cleanup report status is `applied` or verified `up-to-date`.
- [ ] Approved accounts/class and linked test artifacts are absent.
- [ ] `tongminhkhanh`, system owners and dedicated smoke accounts remain.
- [ ] `smoke.student` and `thienkhanh` are attached to `Lớp Smoke Production`.
- [ ] Two private certificate PNG objects are absent from R2.
- [ ] Production smoke passes all 15 checks for admin, teacher, student and parent.
- [ ] D1 audit contains exactly one `PRODUCTION_TEST_DATA_CLEANED` record.
- [ ] Worker, Vercel, migration registry, feature flags, metrics and rollback points are recorded from the final release state.
- [ ] Final documentation PR is merged and tag `v1.0.0` is created from that merge commit.

## Rollback points

- D1: private Time Travel bookmark captured immediately before cleanup.
- Worker: current reviewed version and previous enforce version listed above.
- Vercel: Git-backed production deployment; rollback uses the previous successful deployment or reviewed commit.
- R2: cleanup is limited to two known private certificate object keys. Database cleanup is verified before object deletion.

## Residual product decisions

These are not blockers for closing the modernization engineering plan:

- email delivery remains fail-closed until a provider and SPF/DKIM/DMARC are configured;
- optional product feature audiences may remain disabled or staged through the runtime rollout control plane;
- Dependabot major-version updates are reviewed separately from this release.
