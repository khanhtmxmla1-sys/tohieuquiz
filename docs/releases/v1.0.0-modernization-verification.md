# TôHiệuQuiz v1.0.0 modernization verification

## Release status

**State:** RELEASED — Task 38 production cleanup, post-cleanup smoke and final verification completed on 2026-07-30.

Tag `v1.0.0` is created from the merge commit that contains this record. The GitHub Release metadata is the authoritative exact tagged SHA; the reviewed operational source before this evidence-only closeout is `cb6b41b39201d188bc12ad38c53fd84be5406b01`.

## Source and deployed state

- Reviewed Task 38 preparation PR: #23, merge commit `e8282a7b4c24c9f66d3b802f2177aaa62595806a`.
- Transactional D1.batch hotfix PR: #24, merge commit `cb6b41b39201d188bc12ad38c53fd84be5406b01`.
- Production API Worker: `tohieuquiz-api` version `96705980-78e2-4b5b-89f2-883a989dfec7`, 100% traffic.
- Reviewed Worker rollback version: `2003f752-22fd-4503-a05f-6c377ebfc08a`.
- Vercel deployment for source `cb6b41b39201d188bc12ad38c53fd84be5406b01`: `4mpKdAvtL4dnBh2kfPbeFmQ4adi2`, successful. The final evidence merge is checked by Vercel again before the tag is created.
- Latest production D1 migration: `0054_feature_rollout_control_plane.sql`, applied at `2026-07-30 00:56:09 UTC`.
- Production system settings at closeout: `ai_assistant_enabled=false`; `unified_notifications_v1=true`.
- Runtime control-plane state at closeout: AI assistant disabled (audience all, 100% rule retained); unified notifications enabled for admin audience at 100%.
- Parent email provider remains disabled with SPF/DKIM/DMARC readiness false; this path is intentionally fail-closed and is not a modernization release blocker.
- Task 10 auth observation gates were closed early by owner risk override; the separate auth release records remain authoritative for that exception.

## Cleanup safety and execution evidence

- Cleanup script: `workers/scripts/cleanup-production-test-data.cjs`.
- Runbook: `docs/operations/production-test-data-cleanup.md`.
- Production write uses Cloudflare `D1.batch()` as one transaction through a localhost-only remote development session with a random one-time proof and exact payload digest.
- The first Wrangler file attempt was rejected because remote D1 disallows SQL `BEGIN`; a repeated dry-run confirmed zero mutation before PR #24 introduced the supported batch path.
- Private Time Travel bookmark was captured immediately before cleanup. The full bookmark remains outside Git/CI/chat; recorded SHA-256: `f697567421542d29407f8d1176140fb69a6bb80359cd5da9fd9d2f62b490a5d1`.
- Dry-run target counts: quizzes 1; assignments 2; results 4; certificate batches/certificates 1/2; live-exam sessions 1; result report/batches 1/2; notifications/parent notifications 6/6; private R2 PNG objects 2.
- Cleanup request: `task38-686977b3-12a7-4a5e-a356-c081f13cb458`.
- Applied at: `2026-07-30T10:42:06.634Z` (`2026-07-30 17:42:06 UTC+7`).
- Transaction result: 72 statements, 65 changes, status `applied`.
- Removed exactly the approved `test.gv1`, `test.hs1`, `test.hs2`, `Lớp Test 1` fixtures and their linked quiz/assignment/result/live-exam/report/certificate/notification artifacts.
- Preserved `tongminhkhanh`, `admin`, `viethong`, all dedicated smoke accounts and `thienkhanh`.
- `smoke.student` and `thienkhanh` are active in deterministic class `Lớp Smoke Production`, owned by `smoke.teacher`.
- The two approved private certificate PNG test objects are absent from R2; the certificate template/background remains untouched.
- D1 contains exactly one deterministic `PRODUCTION_TEST_DATA_CLEANED` audit record.
- Independent post-check found target teacher/students/class counts at zero and smoke class count at one.
- Repeated dry-run returned `up-to-date` with zero remaining artifact counts, proving idempotency.
- The ephemeral remote development Worker was absent after the transaction.

## Post-cleanup production verification

- Production smoke run: `30535769458`.
- Source under test: `cb6b41b39201d188bc12ad38c53fd84be5406b01`.
- Started: `2026-07-30T10:43:59.968Z`; finished: `2026-07-30T10:44:28.675Z`.
- Result: `ready`, 15/15 checks passed.
- Passed checks: canonical/apex/parent shells; same-origin health; direct CORS; hostile origin; unauthenticated admin/teacher/student/parent guards; cookie-authenticated admin/teacher/student/parent reads; public browser shell.
- Role-read durations: admin 1,566 ms; teacher 1,526 ms; student 1,308 ms; parent 1,809 ms.
- Ten independent health probes returned HTTP 200; ten unauthenticated protected-route probes returned HTTP 401; observed 5xx: 0.
- Health TTFB average was approximately 209 ms (maximum 417 ms); auth-guard TTFB average approximately 145 ms (maximum 344 ms).
- No cleanup-generated critical or action-required security event was observed. A later admin `SESSION_REVOKED` event was informational and consistent with session lifecycle activity.

## Verification gates

- [x] Private pre-cleanup D1 Time Travel bookmark captured outside the repository.
- [x] Reviewed cleanup PRs merged to `main`.
- [x] Production cleanup report status `applied` and repeated verification status `up-to-date`.
- [x] Approved accounts/class and linked test artifacts absent.
- [x] Owners and dedicated smoke accounts preserved.
- [x] Preserved students attached to `Lớp Smoke Production`.
- [x] Two approved private certificate PNG test objects absent from R2.
- [x] Production smoke passed all 15 checks for admin, teacher, student and parent.
- [x] D1 audit contains exactly one `PRODUCTION_TEST_DATA_CLEANED` record.
- [x] Worker, Vercel, migration, feature flags, metrics and rollback points recorded.
- [x] Final evidence is merged before tag/GitHub Release `v1.0.0` is created from that merge commit.

## Rollback points

- D1: restore with the private pre-cleanup Time Travel bookmark. Use the recorded SHA-256 only to select/verify the privately stored bookmark; never paste the full value into Git, CI or chat.
- Worker: current version `96705980-78e2-4b5b-89f2-883a989dfec7`; reviewed previous enforce version `2003f752-22fd-4503-a05f-6c377ebfc08a`.
- Vercel: Git-backed deployment for `cb6b41b39201d188bc12ad38c53fd84be5406b01` and the immediately preceding successful deployment remain rollback candidates; the final documentation-only deployment does not alter runtime behavior.
- R2: the removed objects were approved test artifacts. If a full cleanup rollback is required, restore D1 first and regenerate only those private certificate objects through the reviewed certificate queue flow.

## Residual product decisions

These are not blockers for closing the modernization engineering plan:

- email delivery remains fail-closed until a provider and SPF/DKIM/DMARC are configured;
- optional product audiences may remain disabled or staged through the runtime rollout control plane;
- Dependabot major-version updates are reviewed separately and are not batch-merged into this release.
